server:
  port: 8002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eamilapi
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create